clear lock
clear control
add control = Caps_Lock Control_L Control_R
! First col after = is the normal action for the keycode; second is the shifted action
keycode 66 = Control_L Control_L NoSymbol NoSymbol
